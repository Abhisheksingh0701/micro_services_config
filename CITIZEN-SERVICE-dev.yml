server:
  port: 8281

spring:
  application:
    name: CITIZEN-SERVICE

  custom:
    message: "Citizen Service Dev-Config"

eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka

      
